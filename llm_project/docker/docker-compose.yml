version: "3.9"

services:
  chatbot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - ../vector_db:/app/vector_db
      - ../data:/app/data
    restart: unless-stopped
